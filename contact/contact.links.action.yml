# All action links for this module

contact.contact_add:
  # Which route will be called by the link
  route_name: contact.contact_add
  title: 'Add Contact'

  # Where will the link appear, defined by route name.
  appears_on:
    - entity.contact.collection
    - entity.contact.canonical
